#include <bits/stdc++.h>
using namespace std;

int main()
{ 
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);  

    int n;
    cin>>n;
    if(n==0)
        cout<<"YONSEI";
    else 
        cout<<"Leading the Way to the Future";
}